export enum EHisTypes {
    HEAD = "head",
    ERROR = "error",
    TEXT = "text",
    CMD = "cmd",
    RESPONSE = "response",
}

export enum ECmd {
    ECHO = "echo",
    CLEAR = "clear",
    MAIN = "main",
    MCP = "mcp",
    RAG = "rag",
}
